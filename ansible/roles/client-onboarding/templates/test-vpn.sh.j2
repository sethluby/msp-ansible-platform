#!/usr/bin/env bash
set -euo pipefail

IF="{{ client_name }}"

if ! command -v wg >/dev/null 2>&1; then
  echo "wg not installed" >&2
  exit 1
fi

wg show "$IF" || { echo "Interface $IF is not up" >&2; exit 2; }
echo "WireGuard interface $IF is up"

