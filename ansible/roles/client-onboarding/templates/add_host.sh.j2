#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

HOSTNAME="${1:-}"
if [[ -z "$HOSTNAME" ]]; then
  echo "Usage: $0 <hostname>" >&2
  exit 1
fi

echo "$HOSTNAME" >> "$SCRIPT_DIR/inventory.ini"
echo "Added $HOSTNAME to inventory.ini"

