---
profile: production

exclude_paths:
  - .cache/
  - .github/
  - molecule/
  - tests/
  - .ansible/
  - venv/
  - .venv/
  - Reference/
  # Exclude vendored third-party roles installed by galaxy during CI
  - ansible/roles/ansible-lockdown.*

skip_list:
  - galaxy[no-changelog]
  - galaxy[version-incorrect]
  - no-changed-when
  - command-instead-of-module
  - run-once[task]

warn_list:
  - experimental
  - yaml[line-length]

offline: true
